
export type PacketStore = {

};

export const initState = {};

function packet (state: any = initState, action: any): PacketStore {
  switch (action.type) {
    case "":
      return {};
    default:
      return {};
  }
}

export default packet;